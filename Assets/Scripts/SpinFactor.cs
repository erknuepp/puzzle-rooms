using Assets.Scripts;

using TMPro;

using UnityEngine;

public class SpinFactor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //GetComponent<TextMeshPro>().text = $"Spin Factor: {Globals.SpinFactor}";
    }
}
